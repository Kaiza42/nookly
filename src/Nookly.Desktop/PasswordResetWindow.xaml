<Window x:Class="Nookly.Desktop.PasswordResetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mot de passe oublie" Height="540" Width="470" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="ResetInput" TargetType="Control">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="11,0" />
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="ResetTextBox" TargetType="TextBox" BasedOn="{StaticResource ResetInput}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ResetPasswordBox" TargetType="PasswordBox" BasedOn="{StaticResource ResetInput}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ResetButton" TargetType="Button">
            <Setter Property="Height" Value="38" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Background" Value="{DynamicResource ThemeAccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="{DynamicResource ThemeAccentHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="{DynamicResource ThemeBackgroundBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}"
            BorderThickness="1" CornerRadius="9">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Button Content="&#xE711;" Click="Close_Click" Width="38" Height="38"
                        HorizontalAlignment="Right" Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource ThemeMutedTextBrush}" FontFamily="Segoe MDL2 Assets"
                        FontSize="11" Cursor="Hand" FocusVisualStyle="{x:Null}" ToolTip="Fermer" />
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="42,20,42,34">
                    <TextBlock Text="Retrouver mon compte" FontSize="25" FontWeight="SemiBold" />
                    <TextBlock Text="Recois un code par email, puis choisis un nouveau mot de passe."
                               Margin="0,8,0,25" Foreground="{DynamicResource ThemeMutedTextBrush}" TextWrapping="Wrap" />
                    <TextBlock Text="1  Recevoir le code" FontSize="15" FontWeight="SemiBold" />
                    <TextBlock Text="Adresse email" Margin="0,13,0,6" />
                    <Grid>
                        <TextBox x:Name="EmailBox" Style="{StaticResource ResetTextBox}" Margin="0,0,128,0" />
                        <Button Content="Envoyer" Click="SendCode_Click" Style="{StaticResource ResetButton}"
                                Width="116" HorizontalAlignment="Right" />
                    </Grid>
                    <Border Height="1" Margin="0,24,0,22" Background="{DynamicResource ThemeBorderBrush}" />
                    <TextBlock Text="2  Choisir un nouveau mot de passe" FontSize="15" FontWeight="SemiBold" />
                    <Grid Margin="0,13,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="118" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Code recu" Margin="0,0,0,6" />
                            <TextBox x:Name="TokenBox" Style="{StaticResource ResetTextBox}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Nouveau mot de passe" Margin="0,0,0,6" />
                            <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource ResetPasswordBox}" />
                        </StackPanel>
                    </Grid>
                    <Border x:Name="StatusSurface" Margin="0,16,0,0" Padding="10,8"
                            Background="{DynamicResource ThemeSurfaceBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}"
                            BorderThickness="1" CornerRadius="5" Visibility="Collapsed">
                        <TextBlock x:Name="StatusText" Foreground="{DynamicResource ThemeTextBrush}" TextWrapping="Wrap" />
                    </Border>
                    <Button Content="Modifier le mot de passe" Click="Reset_Click"
                            Margin="0,18,0,0" Style="{StaticResource ResetButton}" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
