<Window x:Class="Nookly.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Nookly.Desktop.Converters"
        Title="Nookly" Height="720" Width="1120" MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen" Loaded="OnLoaded" Background="#F5F6F8">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:InverseBooleanConverter x:Key="InverseBoolean" />
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="12,2" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#B8C0CC" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="38" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Background" Value="#286C53" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#286C53" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#20242B">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="78" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="60" />
                </Grid.RowDefinitions>
                <TextBlock Text="Nookly" Margin="26,0" VerticalAlignment="Center"
                           Foreground="White" FontSize="24" FontWeight="SemiBold" />
                <StackPanel Grid.Row="1">
                    <Button Content="Bibliotheque" Command="{Binding ShowLibraryCommand}"
                            Style="{StaticResource NavButton}" />
                    <Button Content="Ajouter un media" Command="{Binding OpenDiscoverCommand}"
                            Style="{StaticResource NavButton}" />
                </StackPanel>
                <TextBlock Grid.Row="2" Text="Espace personnel" Margin="26,0"
                           VerticalAlignment="Center" Foreground="#7F8997" FontSize="12" />
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="34,28">
            <!-- Bibliotheque : la recherche filtre uniquement les medias deja ajoutes. -->
            <Grid Visibility="{Binding IsLibraryPage, Converter={StaticResource BoolToVisibility}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Bibliotheque" FontSize="28" FontWeight="SemiBold" />
                        <TextBlock Text="Tes films, series et animes" Margin="0,5,0,0" Foreground="#69717D" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Ajouter" Command="{Binding OpenDiscoverCommand}"
                                Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" />
                        <Button Content="Actualiser" Command="{Binding LoadCommand}"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolean}}"
                                Height="38" Padding="16,0" Background="White" BorderBrush="#D7DBE1" />
                    </StackPanel>
                </Grid>

                <TextBox Grid.Row="1" Text="{Binding LibrarySearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,22,0,0" Height="40" Padding="12,8" BorderBrush="#C9CED6"
                         ToolTip="Rechercher uniquement dans ta bibliotheque" />

                <Border Grid.Row="2" Margin="0,14,0,0" Padding="18" Background="White"
                        BorderBrush="#D7DBE1" BorderThickness="1" CornerRadius="6"
                        Visibility="{Binding IsCreatePanelOpen, Converter={StaticResource BoolToVisibility}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.ColumnSpan="4" Text="Modifier le media" Margin="0,0,0,14"
                                   FontSize="16" FontWeight="SemiBold" />
                        <TextBox Grid.Row="1" Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                                 Height="36" Padding="10,7" Margin="0,0,12,0" />
                        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding MediaTypes}"
                                  SelectedItem="{Binding SelectedMediaType}" DisplayMemberPath="Label"
                                  Height="36" Margin="0,0,12,0" />
                        <TextBox Grid.Row="1" Grid.Column="2"
                                 Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"
                                 Height="36" Padding="10,7" Margin="0,0,12,0" />
                        <StackPanel Grid.Row="1" Grid.Column="3" Orientation="Horizontal">
                            <Button Content="Annuler" Command="{Binding CancelEditCommand}"
                                    Height="36" Padding="13,0" Margin="0,0,8,0" />
                            <Button Content="Enregistrer" Command="{Binding SaveMediaCommand}"
                                    Style="{StaticResource PrimaryButton}" />
                        </StackPanel>
                        <ComboBox Grid.Row="2" ItemsSource="{Binding MediaStatuses}"
                                  SelectedItem="{Binding SelectedMediaStatus}" DisplayMemberPath="Label"
                                  Height="36" Margin="0,12,12,0" />
                        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Ratings}"
                                  SelectedItem="{Binding SelectedRating}" DisplayMemberPath="Label"
                                  Height="36" Margin="0,12,12,0" />
                    </Grid>
                </Border>

                <Grid Grid.Row="3" Margin="0,18,0,0">
                    <ProgressBar Height="3" VerticalAlignment="Top" IsIndeterminate="True"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="Ta bibliotheque est vide" FontSize="18" FontWeight="SemiBold" />
                        <Button Content="Trouver un premier media" Command="{Binding OpenDiscoverCommand}"
                                Style="{StaticResource PrimaryButton}" Margin="0,14,0,0" />
                    </StackPanel>
                    <TextBlock Text="Aucun resultat dans ta bibliotheque."
                               HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#69717D"
                               Visibility="{Binding ShowNoLibraryResults, Converter={StaticResource BoolToVisibility}}" />
                    <ListView ItemsSource="{Binding FilteredItems}"
                              Visibility="{Binding HasFilteredItems, Converter={StaticResource BoolToVisibility}}"
                              Background="Transparent" BorderThickness="0">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Margin" Value="0,0,0,8" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="18,14" Background="White" BorderBrush="#E0E3E8"
                                        BorderThickness="1" CornerRadius="6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="90" />
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="80" />
                                            <ColumnDefinition Width="170" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Description}" Margin="0,4,18,0"
                                                       Foreground="#747D89" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding TypeLabel}" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Text="{Binding StatusLabel}" VerticalAlignment="Center"
                                                   Foreground="#286C53" />
                                        <TextBlock Grid.Column="3" Text="{Binding RatingLabel}" VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="Modifier"
                                                    Command="{Binding DataContext.EditMediaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" Height="30" Padding="10,0" Margin="0,0,6,0" />
                                            <Button Content="Supprimer"
                                                    Command="{Binding DataContext.DeleteMediaCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" Height="30" Padding="10,0"
                                                    Foreground="#8E2F2A" Background="#FFF1F0" BorderBrush="#E6A39E" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Grid>

            <!-- Catalogue externe : aucun resultat n'est ajoute directement. -->
            <Grid Visibility="{Binding IsDiscoverPage, Converter={StaticResource BoolToVisibility}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Text="Ajouter un media" FontSize="28" FontWeight="SemiBold" />
                    <TextBlock Text="Recherche un titre dans le catalogue" Margin="0,5,0,0" Foreground="#69717D" />
                </StackPanel>
                <Grid Grid.Row="1" Margin="0,22,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                             Height="42" Padding="12,9" BorderBrush="#C9CED6" ToolTip="Rechercher sur TMDB" />
                    <Button Grid.Column="1" Content="Rechercher" Command="{Binding SearchCommand}"
                            Style="{StaticResource PrimaryButton}" Height="42" Margin="10,0,0,0" />
                </Grid>
                <StackPanel Grid.Row="2">
                    <ProgressBar Height="3" Margin="0,12,0,0" IsIndeterminate="True"
                                 Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibility}}" />
                    <TextBlock Text="{Binding SearchErrorMessage}" Margin="0,12,0,0" Foreground="#8E2F2A"
                               Visibility="{Binding HasSearchError, Converter={StaticResource BoolToVisibility}}" />
                </StackPanel>
                <ListView Grid.Row="3" ItemsSource="{Binding SearchResults}" Margin="0,18,0,0"
                          Visibility="{Binding HasSearchResults, Converter={StaticResource BoolToVisibility}}"
                          Background="Transparent" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Margin" Value="0,0,0,10" />
                            <Setter Property="Padding" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.ShowSearchResultCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}" Padding="0" Background="Transparent"
                                    BorderThickness="0" HorizontalContentAlignment="Stretch" Cursor="Hand">
                                <Border Padding="12" Background="White" BorderBrush="#E0E3E8"
                                        BorderThickness="1" CornerRadius="6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="64" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>
                                        <Border Height="88" Background="#E4E7EB" CornerRadius="3">
                                            <Image Source="{Binding PosterUrl}" Stretch="UniformToFill" />
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="14,2,20,0">
                                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                                            <TextBlock Margin="0,5,0,0" Foreground="#69717D">
                                                <Run Text="{Binding TypeLabel, Mode=OneWay}" /><Run Text=" - " />
                                                <Run Text="{Binding YearLabel, Mode=OneWay}" /><Run Text=" - " />
                                                <Run Text="{Binding RatingLabel, Mode=OneWay}" />
                                            </TextBlock>
                                            <TextBlock Text="{Binding Description}" Margin="0,7,0,0"
                                                       Foreground="#747D89" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="Voir la fiche" VerticalAlignment="Center"
                                                   HorizontalAlignment="Right" Foreground="#286C53" FontWeight="SemiBold" />
                                    </Grid>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Fiche detaillee avant confirmation de l'ajout. -->
            <Grid Visibility="{Binding IsDetailPage, Converter={StaticResource BoolToVisibility}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Button Content="Retour aux resultats" Command="{Binding BackToDiscoverCommand}"
                        HorizontalAlignment="Left" Height="34" Padding="12,0"
                        Background="White" BorderBrush="#D7DBE1" />
                <Grid Grid.Row="1" Margin="0,24,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="240" Height="360" VerticalAlignment="Top" HorizontalAlignment="Left"
                            Background="#E4E7EB" CornerRadius="6">
                        <Image Source="{Binding SelectedSearchResult.PosterUrl}" Stretch="UniformToFill" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="26,0,0,0">
                        <TextBlock Text="{Binding SelectedSearchResult.Title}" FontSize="30"
                                   FontWeight="SemiBold" TextWrapping="Wrap" />
                        <TextBlock Margin="0,10,0,0" Foreground="#69717D" FontSize="15">
                            <Run Text="{Binding SelectedSearchResult.TypeLabel, Mode=OneWay}" /><Run Text=" - " />
                            <Run Text="{Binding SelectedSearchResult.YearLabel, Mode=OneWay}" /><Run Text=" - Note TMDB " />
                            <Run Text="{Binding SelectedSearchResult.RatingLabel, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock Text="Synopsis" Margin="0,30,0,0" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SelectedSearchResult.Description}" Margin="0,10,0,0"
                                   MaxWidth="620" HorizontalAlignment="Left" Foreground="#4D5967"
                                   FontSize="14" LineHeight="22" TextWrapping="Wrap" />
                        <Button Content="Ajouter a ma liste" Command="{Binding AddSearchResultCommand}"
                                CommandParameter="{Binding SelectedSearchResult}"
                                Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,34,0,0" />
                        <TextBlock Text="{Binding SearchErrorMessage}" Margin="0,12,0,0" Foreground="#8E2F2A"
                                   Visibility="{Binding HasSearchError, Converter={StaticResource BoolToVisibility}}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
